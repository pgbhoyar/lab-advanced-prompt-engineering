"""Advanced Prompt Engineering Workshop — thin CLI reference runner.

This package intentionally stays small. Its job is to make the *prompt* the star:
load shared prompt/manifest/test-case assets, submit one stateless request, display the
response, and validate structured output. It hides no prompt content behind frameworks.
"""

__version__ = "1.0.0"
