# Lab 07 — Challenge (Optional)

> Optional. Does not block the capstone.

1. Run the defensive prompt on `adversarial-indirect-01` (injection hidden in forwarded content).
   Does it resist an *indirect* injection as well as a direct one?
2. Run a consistency check: execute one adversarial case three times. Does the defense hold every
   time? Record any variation rather than keeping only the best run.
3. Write two sentences on what **application-level** controls (input validation, least privilege,
   human confirmation, content safety) you would add on top of the prompt.
