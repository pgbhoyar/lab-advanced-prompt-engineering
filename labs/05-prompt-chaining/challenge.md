# Lab 05 — Challenge (Optional)

> Optional. Does not block later labs.

1. Add a **Stage 3** that drafts a short reply to the requester, running only if Stage 2 assigned
   a category. What new validation would you add between Stage 2 and Stage 3?
2. Deliberately make Stage 1 emit invalid JSON. Confirm the chain stops and reports the failing
   stage instead of continuing.
