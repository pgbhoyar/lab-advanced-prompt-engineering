# Lab 05 — Test Cases

From [`datasets/triage/core-test-cases.jsonl`](../../datasets/triage/core-test-cases.jsonl):

| Case ID | Scenario type | Focus |
|---------|---------------|-------|
| `edge-long-01` | edge-case | Long, noisy message — extraction matters |
| `conflicting-01` | conflicting | Reporter opinion vs policy urgency |

Run the single baseline and the chain on the **same** case to compare.
