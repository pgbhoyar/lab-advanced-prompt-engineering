# Lab 01 — Challenge (Optional)

> Optional. Does not block later labs.

1. Add a constraint to the system prompt that the response must name the **evidence** from the
   request that supports the category. Re-run `normal-01` and check whether groundedness improves.
2. Introduce a deliberate conflict ("be extremely detailed" and "under 40 words"). Observe the
   result, then resolve it by stating an explicit priority order. What changed?
