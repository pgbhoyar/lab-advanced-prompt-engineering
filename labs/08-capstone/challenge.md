# Lab 08 — Challenge (Optional)

> Optional.

1. Add a **conflicting** case (reporter says "not urgent" during a site-wide outage) and confirm
   your prompt applies policy urgency over the reporter's opinion.
2. Version your prompt: make one improvement, bump the version, write `change_notes`, and show the
   before/after rubric scores. Did any case regress?
3. Add a short `known_limitations` list to your manifest describing when a human must review.
