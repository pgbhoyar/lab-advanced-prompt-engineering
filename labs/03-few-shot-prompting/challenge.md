# Lab 03 — Challenge (Optional)

> Optional. Does not block later labs.

1. Add a **fourth** example that intentionally conflicts with the category rules (e.g. label a
   password reset as "hardware"). Re-run and observe how one bad example degrades results. Then
   remove it.
2. Reduce to a single example. Is one example enough for the boundary case, or do you need the
   contrast of multiple categories?
