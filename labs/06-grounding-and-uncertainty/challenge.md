# Lab 06 — Challenge (Optional)

> Optional. Does not block later labs.

1. Create a **conflicting** policy by adding a line that contradicts the shared-mailbox 90-day
   rule. Ask the mailbox question and confirm the improved prompt reports the conflict rather than
   silently picking one rule.
2. Add a line to the policy that contains a fabricated URL. Ask a related question and confirm the
   model does not present that URL as independently verified.
